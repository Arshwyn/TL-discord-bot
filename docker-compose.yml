version: '3.8'

services:
  tl_bot:
    build: .
    container_name: tl_discord_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist SQLite data on your home server
      - ./data:/app/data